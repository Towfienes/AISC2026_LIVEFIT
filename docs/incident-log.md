# Sổ sự cố (incident log)

*Theo HARNESS §3: mỗi lỗi (test đỏ, số liệu bất thường, sự cố trong phiên) ghi MỘT dòng
sau khi đã tìm root cause — không ghi triệu chứng suông. Lỗi trong phiên live ghi thêm
vào nhật ký phiên (`ops/templates/nhat-ky-phien.md`). Cột "gate mới" trả lời câu hỏi
bắt buộc: lỗi này lọt qua gate nào, có cần gate mới không (ghi "không" nếu gate hiện có
đủ). Nhớ: dữ liệu khối/phiên hỏng thì đánh dấu `excluded_reason`, KHÔNG sửa số liệu.*

| Ngày | Triệu chứng | Root cause | Commit fix | Gate mới |
|---|---|---|---|---|
| 25/08/2026 | Scrub lần 2 sinh `[[ĐỊA CHỈ]` lồng nhau (test idempotency đỏ) | Token thay thế `[ĐỊA CHỈ]` chứa chính trigger "địa chỉ" của `ADDR_ANNOUNCE_RE`; trigger ngắn `đc` không yêu cầu dấu `:` nên khớp cả "đc" ≈ "được" | lookbehind `(?<!\[)` + tail loại ngoặc + `đc` bắt buộc có `:` (commit 5111e48) | không — test idempotency đã có sẵn và giữ vĩnh viễn |
| 25/08/2026 | API trong Docker báo `store_backend=memory` dù có DATABASE_URL | docker-compose chỉ đặt `DATABASE_URL`; store chọn backend qua biến `STORE_BACKEND` (mặc định memory) — hai cấu hình tách rời, thiếu một | thêm `STORE_BACKEND: postgres` vào app_env | cân nhắc: smoke-test compose trong CI (job tùy chọn, cần Docker runner) |
| 27/08/2026 | Nút "Xem thử ngay" trên trang chủ lỗi; `/demo/seed` trả HTTP 500 từ lần gọi thứ hai | Hai store phân kỳ hợp đồng: `InMemoryStore.create_shortlink` ghi đè âm thầm nên test (chỉ chạy memory) luôn xanh, còn `PostgresStore` ném UniqueViolation trên mã cố định `demo0/1/2` → 500. Seed lại là thao tác bình thường của người dùng | mã shortlink duy nhất mỗi lần seed + cả hai store cùng ném `ShortlinkCodeTakenError` + route trả 409 kèm retry | CÓ — `tests/test_store_contract.py` (kiểm hợp đồng trên mọi backend, phần postgres chạy khi có DATABASE_URL) và test hồi quy seed hai lần trong `tests/test_api_flow.py` |
| 27/08/2026 | Bàn điều khiển `/desk` đứng im vĩnh viễn dù báo "đang kết nối" | Giao diện gọi 4 đường dẫn không tồn tại (`/host`, `/cards`, `/execute`, `/override` → 404) vì web và API được xây song song, hợp đồng lệch nhau; một lời gọi hỏng làm `Promise.all` bác bỏ cả 4 nguồn, `catch` giữ nguyên màn hình rỗng, `listSessions` vẫn chạy nên UI tưởng kết nối tốt và không rơi về mô phỏng | sửa đường dẫn theo API thật (`/state?role=`, `/actions/*`); `Promise.allSettled` + cảnh báo suy giảm từng vùng | CÓ — `tests/test_web_api_contract.py`: trích mọi đường dẫn từ `web/src/lib/api.ts`, đối chiếu OpenAPI schema; đã kiểm chứng test này FAIL khi cố tình trả lại đường dẫn cũ |
| 27/08/2026 | Trình duyệt của host tải về `assignment` và `seconds_remaining` (rò rỉ làm mù ở tầng mạng) | `getHostState()` gọi `/host` → 404 → `catch` gọi lại `/state` KHÔNG kèm `role=host`, tức nhận payload operator. API che mù đúng; đường đi của web thì không | gọi thẳng `/state?role=host`, bỏ hoàn toàn fallback; thêm lớp lọc phòng thủ loại mọi trường cấm phía client | CÓ — test trong `test_web_api_contract.py` chặn cả `role=host` lẫn việc gọi lại `getState` |
| 27/08/2026 | `livelift-qc` trượt 3/6 trên phiên hoàn toàn sạch chạy qua chính API | Ba nguyên nhân độc lập: (a) luật đòi `inner_propensity` cho MỌI hành động nên can thiệp tay hợp lệ luôn trượt; (b) `event_continuity` dùng thời lượng DỰ KIẾN nên phiên kết thúc sớm báo khoảng trống ảo 3540s; (c) `POST /schedule` không lưu lịch gán vào `design['blocks']` nên `block_integrity` đối chiếu với rỗng; sau đó lộ thêm (d) truy vấn QC không SELECT cột `override_reason` | luật theo `source` (model bắt buộc propensity, human bắt buộc lý do hợp lệ); dùng cửa sổ `end_ts - start_ts` thật; lưu lịch gán verbatim vào design làm dấu vết kiểm chứng; bổ sung cột thiếu trong truy vấn | CÓ — 5 test trong `test_quality.py` + `test_api_robustness.py` kiểm chứng QC đối chiếu được phiên tạo qua API |
| 27/08/2026 | Mọi lệnh CLI treo ~2 phút; `livelift-qc` còn crash `UnicodeEncodeError` giữa chừng | (a) `.env` và mặc định config dùng `localhost`: Windows giải ra `::1` trước, Docker chỉ mở `127.0.0.1:5432`, psycopg chờ hết timeout IPv6 rồi mới fallback — đo được 130.829 ms vs 442 ms (296×); (b) console Windows dùng cp1252 không in được tiếng Việt nên CLI chết giữa báo cáo | chuẩn hóa `localhost`→`127.0.0.1` ở tầng config (kèm sửa `.env`/`.env.example`); thêm `livelift.console.configure()` ép UTF-8, gọi ở đầu mọi CLI | không — hai lỗi đều lộ ngay khi chạy CLI thật; đã ghi cách phát hiện vào README |
| 27/08/2026 | `GET /sessions/nope-123` (và 4 endpoint khác) trả 500 + traceback ASGI | id sai định dạng được đưa thẳng xuống Postgres → `InvalidTextRepresentation`; gõ nhầm URL là lỗi phía client, không phải lỗi máy chủ | kiểm tra UUID một lần trong `service.require_session()` — mọi route theo phiên đều đi qua đây → 404 tiếng Việt | CÓ — `tests/test_api_robustness.py` phủ cả 6 endpoint |
| 30/08/2026 | **FATAL** — hệ thống báo "p < 0.001, có ý nghĩa thống kê" trên dữ liệu THUẦN NHIỄU. Trên 200 phiên 30 phút mô phỏng dưới giả thuyết null: **105 phiên (52%) bị tuyên là có ý nghĩa**, 104 phiên có KTC rộng bằng 0 | `studentized_stat` trả NaN khi một nhánh có dưới 2 khối. Mọi phép so sánh với NaN đều False → đếm được 0 → p sập xuống sàn 1/(draws+1). Cùng biểu thức trong `randomization_ci` trả giá trị dưới alpha cho MỌI tau0 → bisection đẩy hi về lo → KTC = [tau_hat, tau_hat]. Giao diện đọc `ci_low > 0` là TRUE với KTC rộng 0 → hiện "bằng chứng thí nghiệm". Đến được qua API thật: phiên 15 phút cho đúng 3 khối, ràng buộc cân bằng biến mất, all-ON/all-OFF xảy ra được | `MIN_BLOCKS_PER_ARM=2`; `analyze_outer` trả `estimable=False` + lý do tiếng Việt thay vì số; `_p_from_stats` từ chối khi t_obs không hữu hạn; draw suy biến bị LOẠI khỏi phân bố tham chiếu thay vì tính là 0.0; thuộc tính `significant` không bao giờ True khi không ước lượng được | CÓ — 6 test trong `test_estimators.py` gồm test slow chạy end-to-end 120 phiên null. Đo lại sau sửa: **6.2% dương tính giả** (đúng mức 5%), 0 KTC rộng 0 |
| 30/08/2026 | **FATAL** — ước lượng bị suy giảm 43% và CI coverage tụt còn 70% khi phiên kết thúc sớm hơn dự kiến | Lịch giữ nguyên các khối đã lên kế hoạch; `build_ticks` forward-fill số người xem đến hết thời lượng DỰ KIẾN, nên khối chưa từng phát sóng vẫn được bịa ra phơi nhiễm và nhận y = 0.0 cứng. Các số 0 giả này kéo ước lượng về 0 | `block_frame` nhận `live_until_s` (thời gian phát THẬT = end_ts − start_ts), cắt cửa sổ phân tích theo đó; `build_ticks` không phát tick ngoài vùng có telemetry; khối không phát / phơi nhiễm dưới `MIN_EXPOSURE_VIEWER_S` bị đánh dấu `measurable=False` + lý do và bị LOẠI, không bao giờ vào phân tích với y=0; bộ mô phỏng và `true_effect` dùng cùng quy tắc loại để không so sánh lệch estimand | CÓ — `test_unaired_blocks_are_excluded_from_the_report`. Đo lại sau sửa: **suy giảm −7.0%, coverage 93%** |
| 30/08/2026 | **SERIOUS** — bảng lực thống kê (MDE) sai theo cả hai hướng cùng lúc: phóng đại vì tính phương sai cấp phiên hai lần, lại lạc quan vì bỏ qua chênh lệch giữa công thức tiệm cận và kiểm định ngẫu nhiên hóa thật | Ba lỗi độc lập: (a) hệ số thiết kế cụm `1+(m−1)·ICC` là công thức Moulton cho gán CẤP CỤM — ở switchback, can thiệp luân phiên BÊN TRONG phiên nên hiệu ứng phiên tự triệt tiêu trong hiệu ON−OFF; (b) hệ số `(1+ρ)/(1−ρ)` là hiệu chỉnh cho TRUNG BÌNH chuỗi thời gian, áp cho hiệu số giữa các khối ngẫu nhiên hóa xen kẽ là SAI DẤU (tự tương quan dương làm hai trung bình nhánh cùng dao động → giảm phương sai hiệu số); (c) `reports.py` truyền CV GỘP (đã chứa phương sai giữa phiên) rồi lại nhân tiếp hệ số ICC → tính hai lần cùng một phương sai | Bỏ (a) và (b) khỏi mặc định (giữ tham số cho thiết kế gán cấp phiên); thêm `within_session_cv()` và dùng nó trong reports.py. Vì các sửa này làm MDE NHỎ đi — hướng dễ tự tâng bốc — đã ĐO lực thống kê thật: quét 4 mức tác động × 60 lần lặp cho thấy chỉ đạt 65% ở MDE thô, 78% ở 1.15×, 85% ở 1.30× → thêm `RANDOMIZATION_TEST_MARGIN = 1.2` như một hiệu chỉnh ĐO ĐƯỢC. MDE báo cáo: 30.1% (cũ, sai) → 20.1% (đúng, lực 80%) | CÓ — `test_predicted_mde_matches_achieved_power` (slow) đo lực thống kê thật tại MDE dự đoán và fail nếu công thức lệch; `test_randomization_margin_is_applied_by_default` chặn việc công bố số thô |
| 30/08/2026 | **SERIOUS** — tỷ lệ nhấp của khối bị thổi lên tới 26% khi telemetry người xem bắt đầu muộn | `_window_stats` đo tử số và mẫu số trên hai khoảng KHÁC NHAU: lượt nhấp đếm thẳng từ danh sách sự kiện chỉ theo mốc thời gian, còn phơi nhiễm tích phân từ chuỗi tick với `last_viewers = 0.0` trước snapshot đầu tiên → mọi giây trước đó đóng góp 0 phơi nhiễm nhưng vẫn tính nhấp | tử số và mẫu số dùng chung một khoảng đo được (giao của cửa sổ với vùng có telemetry) | gián tiếp — quy tắc phơi nhiễm tối thiểu `MIN_EXPOSURE_VIEWER_S` loại khối thiếu dữ liệu |
| 30/08/2026 | **MODERATE** — ước lượng Horvitz–Thompson dao động theo mất cân bằng nhánh, có thể lệch khỏi hiệu số trung bình nhiều hơn cả tác động đang đo và đổi cả dấu | chia cho `n` thay vì tổng trọng số thực tế; sai số nhận thêm số hạng tỉ lệ với `(n_on − n_off)` nhân MỨC của y — thứ không mang thông tin gì về tác động. Với gán Bernoulli thì số khối mỗi nhánh là ngẫu nhiên | chuẩn hóa Hájek (chia cho tổng trọng số thực tế) | không — đã có test so khớp HT với hiệu số trung bình |
| 30/08/2026 | **MODERATE** — ràng buộc "ít nhất 2 khối/nhánh/giai đoạn" âm thầm suy giảm ở độ dài phiên thông thường | `required = min(min_per_arm_per_phase, len(idx)//2)` bị chặn bởi kích thước giai đoạn; endpoint doubling tiêu 4L timeline cho 2 khối. Đo được: 15/30 phút → đảm bảo 0; 45/60 phút → 1; chỉ từ 90 phút mới đạt 2. Tài liệu và kế hoạch đều tuyên bố là 2 | `Schedule.realized_min_per_arm_per_phase` + `constraint_met`; API trả `warning` tiếng Việt khi lịch không đạt, hiện TRƯỚC khi lên sóng | không — cảnh báo hiển thị ngay khi bốc lịch |
| 30/08/2026 | **SERIOUS** — bằng chứng đầu bảng ("sai lệch 0.5%, coverage 90%") chỉ đo trong thế giới KHÔNG có hiệu ứng lưu, tức đúng giả định mà thiết kế switchback sinh ra để nới lỏng — lập luận vòng tròn | `carryover_halflife_s` mặc định 0 ở cả `SimParams` lẫn `run_validation`; không test nào chạy với hiệu ứng lưu > 0 | thêm 2 test slow chạy với hiệu ứng lưu; ĐO và ghi vào docstring: không hiệu ứng lưu → lệch +0.3% coverage 96%; bán rã 2 phút → lệch −17.4% coverage 96%; bán rã 3 phút → lệch −25.2% coverage 76%. Suy giảm về 0 là hướng THẬN TRỌNG (hệ thống nói giảm chính tác động của mình) | CÓ — `test_estimator_under_carryover_interference` và `test_carryover_attenuates_relative_to_clean_world` |
| 30/08/2026 | **MINOR** — trường `measured_compliance` nằm ngay cạnh bảng lực thống kê đã bỏ qua nó (dùng hằng số 0.95/0.85), và bảng còn giả định CUPED R²=0.3 mà không đoạn code nào ước lượng — cắt 16% MDE dựa trên hư không | tuân thủ đo được không được truyền vào `scenario_table`; các mặc định anti-conservative rơi qua | truyền tuân thủ đo được vào bảng; bỏ giả định R² (mặc định 0) | không |
| 30/08/2026 | **MODERATE** — cổng hiệu chuẩn A/A gần như vô dụng: một kiểm định có tỷ lệ dương tính giả 12% (gấp 2.4 lần mức danh nghĩa) vẫn **qua được cổng 80%** số lần; cổng cũng không có cận dưới nên im lặng trước việc quá bảo thủ | `assert rejection_rate <= 0.15` trên 40 lần lặp: chỉ fail khi ≥7/40 lần bác bỏ. Xác suất qua cổng đo bằng phân phối nhị thức: FPR 10% → 90.0%, FPR 12% → 80.2%, FPR 15% → 60.7%, thậm chí FPR 20% vẫn qua 28.6%. Cổng độ phủ cũng dùng dải cứng [0.88, 0.99] trên 40 lần lặp nên đánh trượt cả ước lượng viên đúng | thay bằng KIỂM ĐỊNH NHỊ THỨC HAI PHÍA chính xác (`scipy.stats.binomtest`) của H0: FPR = α, fail khi p < 0.01, chạy 200 lần lặp. Lực phát hiện đo được: FPR 12% → 88.7% (cũ 19.8%), FPR 15% → 99.2%, quá bảo thủ 1% → 67.7%, báo động giả chỉ 0.8%. Cổng độ phủ cũng chuyển sang kiểm định nhị thức. Kết quả chạy thật: **4.5% bác bỏ (9/200), p=0.872; độ phủ 95.5%** | CÓ — chính cổng này; lực thống kê của nó được tính và ghi trong docstring |
| 06/09/2026 | **BLOCKER** — 0 comment live nào vào được hệ thống: ApiSink gửi khóa `text_scrubbed` trong khi API đòi `text` → mọi POST trả 422 rồi drop sau 3 lần thử; kèm theo: server bỏ `ext_id`/`ts_utc` (restart runner tạo bản ghi trùng, comment sát biên khối gán sai khối), không có đệm bền (mất dữ liệu vĩnh viễn sau ~3.5s), `get_active_live_chat_id` không retry, lỗi 401/403 nền tảng chỉ warning lặp vô hạn | hai phía sink/API mỗi bên có unit test riêng nhưng KHÔNG có test tích hợp nào cho một comment đi xuyên suốt sink→API→store; hợp đồng payload chưa bao giờ được kiểm chéo | thống nhất hợp đồng (`CommentIn.text` + `platform`/`ext_id`/`ts_utc` tùy chọn); idempotency (platform, ext_id) ở cả 2 backend + migration 0002; spool JSONL + CLI `spool_replay`; retry/backoff phân loại 401/403 vs 429/5xx; INGEST_TOKEN cho endpoint ghi | CÓ — `tests/test_ingest_api_integration.py`: ApiSink thật bắn qua ASGI vào app thật, assert text/ts/ext_id vào store và gửi lặp không tạo trùng |
| 06/09/2026 | **SERIOUS (liêm chính thống kê, gói C)** — 4 vết nứt reviewer bắt được trong 5 phút: (1) `/experiment/summary` trả τ̂/p/KTC bất kỳ lúc nào, mâu thuẫn thẳng PREREGISTRATION §7 (cấm nhìn ước lượng trước ngày đóng băng); (2) kiểm định ngẫu nhiên hóa vẽ lại bằng `DesignParams` MẶC ĐỊNH thay vì thiết kế đã lưu của từng phiên → phân bố tham chiếu của một thiết kế không ai chạy; (3) `estimate_ht` trùng đại số với `estimate` tại p=0.5 nhưng được công bố cạnh nhau như 2 ước lượng viên độc lập; (4) hai docstring sai: claim "marginal propensity remains exactly p" chỉ đúng tại p=0.5, và `cuped_adjust` gợi ý đúng hiệp biến mà §5c tuyên bố KHÔNG hợp lệ (previous-block viewers, hậu-can-thiệp) | (1) không có cơ chế cưỡng chế §7 ở tầng API — tiền đăng ký chỉ là văn bản; (2) `_redraw_matrix` gọi `draw_assignments(phases, rng)` với tham số mặc định dù `rebuild_schedule` đã tái tạo được params; (3–4) claim toán học vào docs từ trí nhớ, không kiểm chứng đại số | (1) setting `RESULTS_FREEZE_UNTIL` (fail-closed khi sai định dạng): trước ngày đóng băng summary gạt mọi trường suy diễn theo pattern `estimable=False`, chỉ trả số vận hành; (2) `analyze_outer` nhận `design_params` per-session nối từ `rebuild_design_params`; (3) gỡ `estimate_ht` khỏi báo cáo API + sửa PREREGISTRATION §5b thành ghi chú trung thực; (4) sửa 2 docstring + `draw_assignments` warn khi p≠0.5 kết hợp rerandomization | CÓ — 3 test khóa §7 (đặt/không đặt/sai định dạng), test redraw tôn trọng `min_per_arm_per_phase` đã lưu + test nối wiring, test đẳng thức HT≡DiM tại p hằng, 2 test warning |
| 08/09/2026 | **SERIOUS (đo lường)** — bộ phân loại ý định công bố macro-F1 **0.870** nhưng trên chat BÁN HÀNG THẬT (phiên `b519f75c`, "Mega Live: Achan Shop Hải Phòng", 6.586 bình luận) chỉ đạt **macro-F1 0.271**; accuracy 0.920 còn **THẤP HƠN** baseline tầm thường "luôn đoán `khac`" (0.995). Gán tay 136/932 dự đoán non-`khac` → precision gộp **11.0%** (KTC95 6.8–17.4%), riêng `hoi_size` **0/16 = sai 100%**. Quy chiếu: ~68/932 nhãn hành động là đúng ⇒ radar thổi phồng lượng "khách có ý định mua" **~13–14 lần**. Sai rõ ràng: `EM CHAO CA NHA`→`chot_don` (0.974), `Xoài rẻ quá ạ`→`che_dat`, `Hải phòng toàn đại **gia**`→`hoi_gia`. Trung vị tự tin 0.443 < ngưỡng abstain 0.45 → **51.6%** dự đoán bị đẩy về `khac` | Ba nguyên nhân độc lập, không cái nào là "ngưỡng chưa chỉnh": (a) **bộ nhãn thiếu lớp lớn** — đo trên 200 mẫu ngẫu nhiên: 40.0% chat là xã giao (chào hỏi 10.0% + cảm ơn/khen 28.0% + kêu gọi 2.0%) và 5.0% là câu hỏi thương mại thật của khách nằm ngoài 6 lớp (hỏi sản phẩm 2.0%, hỏi đại lý 2.0%, hối đơn cũ + góp ý 1.0%) — gấp 10 lần số ý định model bắt được; tất cả bị dồn vào `khac` nên model không có chỗ đúng để đặt chúng; (b) **đa nghĩa tiếng Việt** mà bộ biên soạn 320 câu không hề có cặp đối nghịch: `đắt`(giá) vs `bán đắt`(chạy), `cao`(giá) vs `cao`(chiều cao), `gia` trong `đại gia`, `đơn`(hàng) vs `đơn`(kiện); (c) **không có mô hình về NGƯỜI NÓI** — bảng giá do mod tự dán chiếm 13/30 dương tính giả của `hoi_gia`. Gốc rễ chung: benchmark 0.870 đo **cùng phân phối** với dữ liệu huấn luyện tự biên soạn, chưa bao giờ có phép đo nào trên chat thật đúng miền (live-fire 02/09 là stream cờ vua tiếng Anh — chỉ kiểm được hành vi ngoài miền). Phát hiện kèm: **34/60 dòng `khac` của chính bộ biên soạn** thuộc lớp khác theo guideline mới → dataset tự mâu thuẫn, cấm train lại trước khi gán nhãn lại | (1) mở rộng bộ nhãn 6→11 lớp có định nghĩa + ví dụ THẬT nguyên văn (`chao_hoi`, `cam_on_khen`, `hoi_sanpham`, `hoi_daily`, `bao_gia_shop`); (2) gom bộ nhãn về **MỘT nguồn** `src/livelift/nlp/labels.py` (trước đó chép ở 4 chỗ: `intent.INTENT_LABELS`, `train_intent.LABELS`, `label_llm.LABEL_GUIDELINE`, chuỗi cứng "6 lớp"), tách `INTENT_LABELS` (gán nhãn) khỏi `TRAINED_LABELS` (artifact thật sự dự đoán được); (3) protocol lô hai tầng `--random-fraction` — mẫu ngẫu nhiên đơn giản cho prevalence **không chệch** + phần còn lại uncertain-first; (4) xuất **1.800 nhãn** từ đúng phiên này (`data/labeling/lot1-achan-b519f75c/`); (5) **KHÔNG train lại** (chưa có nhãn) và **KHÔNG chỉnh ngưỡng 0.45** — chỉnh ngưỡng lúc này chỉ đổi chỗ lỗi vì gốc là bộ nhãn thiếu lớp. Ghi số trung thực: `docs/benchmarks/live-fire-achan.md` | CÓ — `test_label_set_comes_from_exactly_one_module` (kiểm `is`, không phải `==`, nên hai bản sao không thể lệch), `test_class_count_in_prompt_is_computed_not_hardcoded`, `test_trained_labels_match_the_shipped_artifact` (chặn model bịa lớp chưa học), `test_khac_examples_do_not_contradict_the_new_classes` (bắt đúng lỗi prompt dạy ngược đã suýt lọt vào lô đầu), `test_live_fire_classes_are_declared_with_guideline_and_real_examples`. **Cổng quy trình mới:** mọi con số benchmark của bộ phân loại chỉ được công bố kèm nguồn phân phối, và con số bộ biên soạn phải đi kèm con số chat thật — không nêu một mình |
| 09/09/2026 | **MODERATE (bằng chứng vòng tròn)** — mọi con số A/A và độ phủ đã công bố đều đo trong thế giới KHÔNG có phân cụm ở biến kết quả (ICC đo lại được: ≈0,01), dù docstring `sim/simulator.py` ghi rằng cú sốc phiên "generates session ICC" và PREREGISTRATION §3 bước 1(d) đăng ký hẳn việc ĐO ICC cấp phiên | Biến kết quả chính là TỶ LỆ (click/1000 giây·người xem). `session_shock_sd` nhân vào tốc độ LƯỢT VÀO, tức nhân cả tử số lẫn mẫu số, nên nó đổi độ nhiễu của y chứ không đổi E[y] — không sinh ICC. Docstring nói ngược lại nên không ai đi tìm knob thật; hệ quả quy trình: bước 1(d) đo ICC rồi bước 2 (power curve trên simulator) không có chỗ nạp vào, ICC đo được bị vứt | Sửa docstring; thêm knob `session_click_sigma` (nhân tử cấp phiên mean-1 lên xác suất nhấp, luồng RNG riêng, mặc định TẮT) và `click_frailty_cv`; ĐO ánh xạ σ→ICC trên 400 phiên (σ=0,06 → ICC 0,048 ± 0,008) và ghi bảng vào docstring; PREREGISTRATION §3 thêm trục ICC vào lưới hiệu chỉnh. Chạy lại chính hai cổng cũ trong thế giới ICC≈0,05 với NGUYÊN ngưỡng: **XANH** — đúng cơ chế (redraw ngẫu nhiên hoá diễn ra trong từng phiên nên mức nền cấp phiên triệt tiêu khỏi tương phản) | CÓ — `test_aa_false_positive_rate_near_alpha_under_session_icc` + `test_effect_recovery_bias_and_coverage_under_session_icc` (slow); `tests/test_sim_heterogeneity.py` khoá bất biến từng-bit của seed cũ và ánh xạ σ→ICC |
| 09/09/2026 | **MINOR (cổng không có răng)** — bộ khung lưới SBC mới ra 4/4 ô XANH, nhưng "toàn xanh" một mình không phân biệt được "đường ống đã hiệu chuẩn" với "phép kiểm không thể đỏ" | Không có kiểm chứng nghịch đảo. Thử nghiệm đầu: tắt studentization ở CẢ HAI phía — và phát hiện phép tiêm này KHÔNG phát hiện được, vì kiểm định ngẫu nhiên hoá là chính xác dưới giả thuyết không sắc với BẤT KỲ thống kê nào, nên ô A/A vẫn đồng đều | Đổi sang lỗi thật sự phát hiện được: lệch pha giữa thống kê QUAN SÁT và thống kê THAM CHIẾU (một phía mất studentization) → p-value dồn về 1 → ô ĐỎ trên tiêu chí đồng đều. Có cả đối chứng dương (đường ống nguyên vẹn, cùng seed → XANH) | CÓ — `test_injected_statistic_mismatch_gives_a_red_cell` + `test_intact_pipeline_gives_a_green_cell` (slow) trong `tests/test_sim_report.py` |
| 10/09/2026 | **MAJOR (nhận vơ năng lực)** — live-fire đa nguồn: phiên replay `TdLWyV3hNao` có **0 bình luận, 0 người xem, 1.430 dòng tick toàn số 0** nhưng `GET /sessions/{id}/signals` báo `ticks → ok "1430 điểm đo, phủ 100%"` và năng lực "nhịp phiên (người xem theo thời gian)" → `ok — đủ tín hiệu`. Lỗi bật ở MỌI phiên replay, kể cả 6.586 bình luận của Achan | `core.signals.assess()` chấm tín hiệu `ticks` bằng SỐ DÒNG tick, trong khi `ticks` theo định nghĩa là telemetry NGƯỜI XEM. Đường replay ghi mỗi 30 s một dòng để mang nhịp bình luận và điền `viewers=0.0` vì VOD đã kết thúc không còn lộ số người xem — đúng như `synth_ticks_from_comments` đã cảnh báo ("must be labeled as unavailable in any UI"). Lớp signals đọc số dòng chứ không đọc nội dung nên chỗ trống thành phép đo; mâu thuẫn thẳng với hợp đồng đã ghi trong `youtube_ytdlp.iter_viewers` (không có số người xem ⇒ `ticks` phải `missing`) | `assess()` thêm tham số **bắt buộc** `n_ticks_with_viewers` (không mặc định — mọi caller phải khai đã đo gì): 0 dòng có người xem ⇒ `ticks=missing` kèm giải thích tiếng Việt, một phần có ⇒ `degraded` theo tích `phủ × tỷ lệ dòng có người xem`; route `/signals` đếm `viewers > 0` từ store | CÓ — `test_comment_tempo_ticks_are_not_counted_as_viewer_telemetry` + `test_partially_missing_viewer_numbers_degrade_the_ticks_signal` (`tests/test_signals.py`) và `test_signals_endpoint_refuses_to_claim_viewers_from_tempo_only_ticks` (end-to-end qua API) |
| 10/09/2026 | **MINOR (im lặng khi rỗng)** — `POST /replays/youtube` với video `TdLWyV3hNao` (715 phút) trả `status=done, n_comments=0, detail=null`: người vận hành nhận một phiên trông khoẻ mạnh và rỗng không, không một dòng cảnh báo | Metadata YouTube quảng cáo track `live_chat` KHÔNG bảo đảm track đó có nội dung — một trạng thái thật mà mã chưa có tên gọi. `_run_job` chỉ đặt `detail` cho nhánh cắt bớt; nhánh "tải được nhưng chat rỗng" rơi chung vào `detail=None` với nhánh thành công bình thường | Hằng `EMPTY_CHAT_DETAIL` đặt khi `not comments`; giữ `status=done` (tải thành công, kết quả rỗng là kết quả hợp lệ) nhưng NÓI RA. Kết hợp với fix cùng ngày ở trên, phiên rỗng nay báo cả 5 năng lực `missing` | CÓ — `test_replay_route_says_so_when_the_chat_replay_is_empty` (`tests/test_youtube_replay.py`), kiểm cả job detail lẫn ma trận tín hiệu |
| 11/09/2026 | **FATAL (mất dữ liệu, ĐÃ XẢY RA THẬT)** — lúc **13:05:53** tiến trình API khởi động lại và **13 phiên live thật + 17.535 bình luận biến mất vĩnh viễn**; kiểm kê lúc 13:13 chỉ còn 4 phiên `demo/seed` với 160 bình luận. Không khôi phục được. Sau đó còn mất thêm một lần nữa. Quy ra sản phẩm thật: một KOL live 90 phút mà máy chủ restart ở phút 89 là mất trắng cả buổi (`docs/benchmarks/kiem-chung-van-hanh.md` mục 0) | Kho đang chạy `store_backend=memory`: toàn bộ dữ liệu nằm trong RAM của đúng tiến trình đó, nên restart = mất sạch, đúng như thiết kế. Ba nguyên nhân độc lập khiến rủi ro này tồn tại mà không ai chặn: (a) chế độ memory **không có bất kỳ đường ghi xuống đĩa nào** — mặc định an toàn lẽ ra phải là *có lưới*, không phải *không có gì*; (b) `/health` chỉ trả `store_backend` chứ **không nói dữ liệu có bền vững hay không**, nên chế độ mất dữ liệu hoàn toàn vô hình — phải ngồi đếm phiên bằng tay mới phát hiện; (c) đường Postgres đã có sẵn nhưng **khó bật đúng**: `DATABASE_URL` chỉ nói cơ sở dữ liệu ở đâu, còn `STORE_BACKEND` mới quyết định có dùng hay không (chính cái bẫy của sự cố 25/08/2026), nên đặt một biến mà quên biến kia thì hệ thống chạy vui vẻ trên RAM | (1) **Đường chính — Postgres, kiểm chứng thật**: dựng `docker compose up -d db`, `livelift-migrate up` (áp 2→7), chạy `tests/test_store_contract.py` với DATABASE_URL thật (**21 test xanh, 10 test nhánh `postgres` thực sự được thu thập và chạy**), rồi nạp một phiên qua API và **giết cứng** tiến trình: dữ liệu còn nguyên 25/25 bình luận. Thêm `scripts/bat_postgres.py` bật cả đường bằng một lệnh và in ra biến còn thiếu; `build_store()` nay cảnh báo tiếng Việt khi có `DATABASE_URL` mà thiếu `STORE_BACKEND`. (2) **Đường dự phòng — ảnh chụp cho kho memory** (`SnapshotManager`, mặc định BẬT): mỗi 30 giây (cấu hình được) tuần tự hoá TOÀN BỘ trạng thái và ghi **nguyên tử** (tệp tạm → `fsync` → `os.replace`), chỉ ghi khi kho đổi (bộ đếm `_rev`), nạp lại lúc khởi động, chụp lần cuối lúc tắt; tệp hỏng bị cách ly thành `*.hong-<ts>` chứ không ghi đè. Đo đúng quy mô đã mất (13 phiên/17.535 bình luận/2.340 tick): tuần tự hoá **107 ms** (phần khoá event loop), tổng **242 ms**, tệp **6,30 MB**, nạp lại **102 ms**, khớp **100%** mọi bảng. Đo cửa sổ mất mát thật: chu kỳ 5 s, nạp liên tục 17,4 bình luận/giây rồi giết cứng giữa dòng → giữ được **760/783 (97,1%)**, mất **23 bình luận = 1,3 giây dữ liệu**, trong trần một chu kỳ. (3) **Nói ra sự thật**: `/health` thêm `storage_mode` / `durable` / `storage_warning` — chế độ memory trần trả thẳng cảnh báo dẫn đúng con số 17.535 đã mất. (4) `docs/luu-tru-du-lieu.md` + `STORE_BACKEND` và `STORE_SNAPSHOT_*` vào `.env.example` | CÓ — `tests/test_store_snapshot.py` (**24 test**): khôi phục đủ 12 bảng, mốc thời gian phải trở lại kiểu `datetime` (không phải chuỗi ISO), `Decimal` giữ nguyên kiểu, chỉ mục chống trùng `(platform, ext_id)` được dựng lại đúng nên restart không nhân đôi bình luận, ghi nguyên tử giữ bản cũ + không để lại tệp tạm, lỗi ghi không giết API, vòng lặp chỉ ghi khi có thay đổi, tắt ảnh chụp thì không đụng đĩa, `/health` bắt buộc cảnh báo, và **hai cổng cấu trúc chặn lỗi tương lai**: mọi phương thức ghi phải tăng `_rev` (quên ⇒ thay đổi không bao giờ được chụp) và mọi bảng dữ liệu phải nằm trong ảnh chụp (thêm bảng mà quên xuất ⇒ mất âm thầm). cộng một cổng chặn **ghi lùi**: bản ảnh chụp cũ (từ vòng lặp định kỳ đang ghi dở ở luồng phụ) không được đè lên bản mới hơn của cú chụp lúc tắt — nếu không thì chính bản vá chống mất dữ liệu lại làm mất dữ liệu. **Đã kiểm chứng cổng có răng:** tiêm 7 lỗi (quên `_rev`, ghi đè không nguyên tử, quên xuất bảng `comments`, lưu thời gian thành chuỗi trần, `/health` im lặng, bỏ chốt chặn ghi lùi, bỏ cảnh báo cấu hình) → cả 7 đều làm đỏ đúng test tương ứng. Thêm cổng vận hành `scripts/kiem_chung_ben_vung.py`: giết cứng tiến trình rồi đếm, **có đối chứng âm** (`--no-snapshot` PHẢI mất dữ liệu — và đã mất đúng như vậy, tái hiện nguyên văn sự cố này) |
| 12/09/2026 | **BLOCKER (chặn đường vận hành, gói A)** — `POST /sessions/{id}/schedule` trả HTTP 500 **rỗng** và **tất định** (20/20 seed ở cấu hình (5 phút, khối 1 phút)) với mọi bố cục ra đúng **5 khối đo**: 5/1, 10/2, 15/3, 20/4, 25/5, 50/10, 75/15 — tức cả dải `5 ≤ planned_duration_min / block_min < 6`, không chỉ tỷ số đúng bằng 5 như báo cáo hiện trường ghi. Quét thuần 1–180 phút × 6 độ dài khối: **36/1.080 tổ hợp** ném `RuntimeError` (→ 500) và thêm **30 tổ hợp** `duration < block_min` ném `ValueError` trần (cũng → 500). "Phiên 50 phút, đổi khối mỗi 10 phút" nằm ngay giữa dải hỏng | `_transition_requirement` hạ ràng buộc cặp-chuyển-tiếp bằng một **cận CẦN dạng đóng** (`k ≤ (n_pairs − forced_switches)/2`) bỏ qua tương tác giữa các switch bị cân bằng nhánh ép ra và cấu trúc run của chuỗi. Với `phases = [early, early, mid, late, late]` nó hứa k = 1 trong khi **tập chấp nhận RỖNG**: cân bằng nhánh ép `arms[0] ≠ arms[1]` và `arms[3] ≠ arms[4]`, nên hai cặp liền kề duy nhất còn có thể cùng nhánh là (1,2) và (2,3) — chúng dùng chung khối 2 nên KHÔNG thể đồng thời là một cặp (BẬT,BẬT) và một cặp (TẮT,TẮT). `draw_assignments` vì thế đốt hết 10.000 lần vẽ lại rồi ném `RuntimeError`, và route không bắt ngoại lệ nào nên ASGI trả 500 không thân. Đối chiếu vét cạn: cận cũ **hứa quá tay ở 8 bố cục** trong không gian 3-giai-đoạn liền kề tới 13 khối. Nguyên nhân sâu hơn là nguyên tắc: một cận CẦN được dùng như thể nó ĐỦ | (1) thay cận xấp xỉ bằng **tìm kiếm khả thi CHÍNH XÁC** `_acceptance_set_nonempty` — quy hoạch động dựng nhân chứng, cắt tỉa chỉ theo hướng bảo thủ, `lru_cache` vì kiểm định ngẫu nhiên hoá vẽ lại hàng trăm lần/phiên (500 lịch 90 phút: 0,03 s); bố cục 5 khối nay hạ đúng về k = 0, sinh lịch bình thường kèm cảnh báo tiếng Việt TRƯỚC khi lên sóng, nêu độ dài khối thay thế **cụ thể** (phiên 50 phút → khối 4 phút); (2) `ScheduleInfeasibleError` (kèm giải thích + cấu hình thay thế) và `RerandomizationExhaustedError` thay cho `ValueError`/`RuntimeError` trần, route `POST /schedule` trả **400** tiếng Việt thay vì 500 câm. Đo lại: 5–240 phút × 9 khối × 2 washout = **4.028 lịch sinh được, 220 tổ hợp 400, 0 tổ hợp 5xx** | CÓ — `tests/test_schedule_config_matrix.py` (14 test): quét thuần toàn không gian cấu hình; quét **HTTP** lưới 6 thời lượng × 4 độ dài khối khẳng định không tổ hợp nào 5xx (hoặc 200, hoặc 400 có nội dung); `test_transition_requirement_matches_exhaustive_search` đối chiếu **3.432 bố cục** với liệt kê vét cạn — đỏ cả khi hứa quá tay lẫn khi hạ quá tay. **Đã kiểm chứng cổng có răng:** khôi phục cận cũ → 12/14 test đỏ; bỏ handler 400 ở route → test 400 đỏ |
| 12/09/2026 | **SERIOUS (thí nghiệm rỗng ruột, im lặng — gói A)** — phiên `mode: "auto"` với 4 khối BẬT chạy trọn vẹn mà **không khối nào được can thiệp**: `compliance_rate = 0,0`, `diff_in_means = null`, trong khi **mọi endpoint vẫn trả 200 và không một cảnh báo nào**. `grep` xác nhận không scheduler/worker nào gọi `execute_action` — "auto" chỉ là cái nhãn trên bản ghi phiên | Hai lỗ hổng độc lập, không cái nào là "quên gọi API": (a) **không tồn tại** bộ thực thi phía máy chủ — `execute_action` chỉ chạy khi có người bấm, nên chế độ auto về bản chất giống hệt chế độ suggest; (b) **không có cơ chế phát hiện** — không tầng nào (signals, report, state) đối chiếu "số khối BẬT đã phát" với "số exposure_event", nên trạng thái 0 hành động không phân biệt được với trạng thái khoẻ mạnh. Nhánh can thiệp chưa hề được can thiệp thì thí nghiệm không phải yếu — nó không tồn tại | `src/livelift/api/autopilot.py`: task asyncio trong lifespan, quét phiên live chế độ auto mỗi `LIVELIFT_AUTOPILOT_TICK_S` giây và khi vào khối BẬT thì gọi **đúng** `routes.actions.execute_action` (giữ nguyên inner propensity, `candidates_json`, `intervention_log`, `exposure_event` — không có đường ghim thứ hai để trôi lệch). **Idempotent theo `exposure_event.block_idx`**: khởi động lại máy chủ giữa phiên không ghim trùng và không bỏ sót khối đang phát; cùng một luật khiến autopilot **tránh** khối mà người vận hành vừa thao tác. **Khối TẮT: hands-off tuyệt đối** — KHÔNG tự gỡ ghim, vì `execute_action` cấm hành động trong khối TẮT và `override` đòi một trong ba lý do an toàn của CON NGƯỜI; robot khai "hết hàng" để gỡ ghim sẽ là bản ghi kiểm chứng giả. Khối BẬT đã trôi qua **không ghim bù** (sẽ bị quy sai khối) mà được liệt kê trong `missed_on_blocks`. Log tiếng Việt mỗi lần tự ghim + nhịp tim + panel `autopilot` trên `GET /state?role=operator` (KHÔNG lên `HostState` — rule L6, panel nêu chỉ số khối). Tắt bằng `LIVELIFT_AUTOPILOT=0`, **mặc định BẬT**. **Báo động** (`LIVELIFT_AUTOPILOT_SILENCE_MIN`, mặc định 5 phút) tính từ **kho exposure**, không từ nhịp tim, nên vẫn kêu khi bộ thực thi bị tắt hoặc chưa từng khởi động. **Hạn chế đã ghi rõ:** vòng quét nằm trong tiến trình API — chạy MỘT worker, hoặc đặt `LIVELIFT_AUTOPILOT=0` ở mọi worker trừ một; khoá chống đua nhiều tiến trình (advisory lock) là việc tiếp theo | CÓ — `tests/test_autopilot.py` (19 test), ép nhịp thời gian bằng cách dời `start_ts` trong kho (đồng hồ máy chủ vẫn thật, KHÔNG sleep): phiên auto đi qua **toàn bộ** biến khối phải cho `compliance_rate = 1,0`, số `exposure_event` khớp đúng số khối BẬT và **0 exposure nào rơi vào khối TẮT**; quét lặp trong cùng khối ghim đúng 1 lần; giả lập khởi động lại (xoá nhịp tim) không trùng/không sót; override tay thắng; khối đã trôi qua vào `missed_on_blocks`; và **cổng báo động**: phiên auto live 12 phút, 0 hành động → `/state` phải trả `alarm` chứa "BÁO ĐỘNG" (kể cả khi `LIVELIFT_AUTOPILOT=0`), còn phiên đang chạy tốt thì `alarm = null`. **Đã kiểm chứng cổng có răng:** vô hiệu hoá `step_session` + `silence_alarm` → **9/19 test đỏ** |
| 12/09/2026 | **SERIOUS (hai màn hình, hai sự thật — gói C)** — cùng MỘT phiên: `GET /sessions/{id}/signals` báo `clicks → ok "93 lượt nhấp qua link đo"` và năng lực `thí nghiệm nhân quả (BẬT/TẮT) → ok — đủ tín hiệu`, trong khi `GET /sessions/{id}/report` của đúng phiên đó cho `clicks: 0` ở MỌI khối và `diff_in_means: null`. Chủ shop được bảo "đủ tín hiệu" rồi nhận về số rỗng, không một lời giải thích (`docs/benchmarks/kiem-chung-van-hanh.md` §2.4a) | `_signal_coverage` truyền `n_clicks = len(store.list_clicks(...))` — SỐ DÒNG click — trong khi biến kết quả chính (tiền đăng ký §4.1), `core.features.block_frame` và `/bao-cao` đều đếm click **HỢP LỆ** (`is_valid`). 93 cú bấm của kịch bản đều mang `python-urllib` trong User-Agent nên bộ lọc GIVT-lite gắn cờ đúng cả 93; tầng signals không hề đọc cờ ấy. Cùng một chữ "lượt nhấp" trỏ vào **hai đại lượng khác nhau** ở hai đầu của cùng một API — đúng hình thái lỗi của sự cố 10/09 (chấm tín hiệu bằng số DÒNG thay vì nội dung), lặp lại trên một tín hiệu khác | `core.signals.assess()` bỏ `n_clicks`, thay bằng **hai tham số bắt buộc** `n_clicks_valid` + `n_clicks_raw` (không mặc định — caller nào cũng phải khai đã đo gì). Trạng thái chấm trên số HỢP LỆ: 0 hợp lệ trên >0 thô ⇒ `missing` kèm câu nói thẳng bộ lọc đã gắn cờ toàn bộ; >50% bị gắn cờ ⇒ `degraded`; còn lại ⇒ `ok`. Số thô KHÔNG bị giấu: nó đi kèm ở trường mới `SignalState.secondary` với nhãn riêng "số phụ bắt buộc báo cáo kèm (§4.1)", đúng như §4.1 yêu cầu. `/bao-cao` thêm `luot_nhap_tho` để hai màn hình mang cùng một CẶP số. Hệ quả đúng: năng lực "thí nghiệm nhân quả" không còn báo `ok` khi tử số rỗng | CÓ — 4 test đơn vị trong `tests/test_signals.py` (toàn bộ bị gắn cờ ⇒ missing + năng lực missing; câu chính dẫn bằng số hợp lệ còn số thô nằm ở `secondary`; đa số bị gắn cờ ⇒ degraded; "chưa có link đo" vẫn phân biệt được với "có link, 0 hợp lệ") **cộng một cổng end-to-end** `test_signals_endpoint_reports_the_same_click_number_as_bao_cao` (4 người xem thật + 3 bot trên cùng link đo, hỏi `/signals` và `/bao-cao` độc lập rồi đối chiếu), và `test_case_2_signal_matrix_does_not_overstate_clicks_when_bots_click`. **Đã kiểm chứng cổng có răng:** trả `/signals` về đếm thô → 2 test end-to-end đỏ, test đơn vị vẫn xanh — chính là lý do cổng end-to-end phải tồn tại |
| 12/09/2026 | **SERIOUS (thông báo lỗi nói sai sự thật — gói C)** — ghim một sản phẩm **vừa tạo, `stock = 50`**, bị từ chối 409 với câu *"Thẻ không còn hợp lệ — sản phẩm đã hết hàng hoặc danh sách gợi ý vừa thay đổi. Chờ thẻ mới rồi thử lại."* Cả hai lý do đều SAI (hàng còn 50, danh sách gợi ý không đổi) và lời khuyên là không bao giờ thực hiện được — danh sách sẽ không tự biến thành sản phẩm đó. Chính cái 409 này kéo tuân thủ của phiên kiểm chứng xuống 25% thay vì 50% (§2.4b) | `execute_action` dựng tập ứng viên bằng `build_candidates(...)` với `top_k = MAX_CARDS = 3` — một **giới hạn HIỂN THỊ** — rồi dùng chính tập bị cắt đó làm tập **ĐỦ ĐIỀU KIỆN**. `scope_candidates_to_product` trả `None` cho mọi sản phẩm ngoài top-3, và MỘT câu thông báo duy nhất phục vụ ba tình huống khác hẳn nhau: mã không có trong kho, sản phẩm hết hàng thật, và sản phẩm còn hàng nhưng xếp ngoài top-3. Ở trạng thái nguội mọi sản phẩm mang đúng prior Gamma-Poisson nên top-3 được quyết bởi thứ tự `product_id` — sản phẩm mới thua vì chữ cái, một luật không ai đoán ra và không đổi được bằng cách chờ | Tách ĐỦ ĐIỀU KIỆN khỏi HIỂN THỊ: route xếp hạng TOÀN BỘ sản phẩm còn hàng (`top_k = len(products)`) rồi mới cắt `MAX_CARDS` cho tập ngẫu nhiên hoá — nhờ vậy câu từ chối nói được vị trí THẬT. `refusal_for_unpinnable_product()` trả ba câu cho ba sự thật: **404** khi mã không có trong kho (nêu đúng mã); **409** "đang HẾT HÀNG (tồn kho 0)" khi thật sự hết hàng; **409** nêu tồn kho thật + thứ hạng thật ("đứng thứ 4/5") + luật xếp hạng + đường đi thật sự tồn tại khi sản phẩm còn hàng mà ngoài tập gợi ý. Phân biệt thêm hai cách bấm: bàn điều khiển bấm một **thẻ đã render** thì "tải lại `/state`" là lời khuyên làm được thật, còn client gõ thẳng `product_id` thì được chỉ sang `/actions/override` **kèm cảnh báo rằng đó là can thiệp của NGƯỜI và không tính vào tuân thủ nhánh BẬT** — không hứa hão lần nữa. Câu "phiên không ở trạng thái cho phép" nay dùng chung một nguồn với danh sách thẻ rỗng (`cards.pin_cards_blocked_reason`) nên bàn và máy chủ không thể kể hai chuyện | CÓ — 5 test hàm thuần trong `tests/test_actions_scoping.py` (mỗi tình huống một câu; `test_every_refusal_names_a_different_cause` khẳng định ba câu KHÁC NHAU; hai test cấm chuỗi "hết hàng" và "Chờ thẻ mới rồi thử lại" xuất hiện sai chỗ), `test_execute_scopes_to_the_clicked_card` nay đòi đúng "tồn kho 0", và `test_case_9_pinning_a_product_you_just_created_is_answered_truthfully`. **Đã kiểm chứng cổng có răng:** khôi phục câu chung chung cũ → **7/13 test đỏ** |
| 12/09/2026 | **SERIOUS (không có đường thoát — gói C)** — phiên chưa từng `start` thì `POST /sessions/{id}/end` trả 409 *"Phiên không ở trạng thái đang phát"* và phiên **kẹt ở `planned` vĩnh viễn** (`start_ts: null`, `end_ts: null`). Danh sách phiên của chủ shop tích tụ những dòng "đang chờ" không bao giờ đóng; đường vòng duy nhất là sinh lịch gán + bấm phát sóng rồi mới kết thúc — tức bắt người dùng **giả vờ chạy một thí nghiệm** để đóng được một phiên quan sát (§3.3) | Vòng đời phiên chỉ có bốn trạng thái `planned → scheduled → live → ended` và **không có trạng thái cuối nào cho phiên chưa lên sóng**. `end_session` đòi `status == "live"` vì `ended` mang nghĩa "buổi phát đã diễn ra và đã kết thúc" — dùng nó cho phiên chưa phát sẽ nói dối đúng cái trường mà bộ lọc mẫu phân tích đọc. Chế độ quan sát (nhập bình luận tay khi phiên còn `planned`) là đường đi hợp lệ và hữu ích, nhưng nó không có cửa ra | Migration 0008 mở `status` thêm **`cancelled`** = "đã đóng mà KHÔNG phát sóng", kèm CHECK `status <> 'cancelled' OR start_ts IS NULL` để một phiên đã huỷ không bao giờ mang mốc phát sóng. `POST /end` nay tự làm đúng việc: phiên đang phát ⇒ `ended`, phiên chưa phát ⇒ `cancelled` (không bắt người dùng đoán ra một đường thứ hai), và `POST /sessions/{id}/cancel` là đường tường minh, **idempotent**. Hai chiều đều khoá: phiên `cancelled` không `start`/`schedule` lại được, phiên đã phát không huỷ được (buổi phát đã diễn ra là sự thật, không xoá bằng một nút bấm). Ý nghĩa khoa học giữ nguyên **bằng cấu trúc**: phiên huỷ không có `start_ts` nên không có khối đo nào, và PREREGISTRATION §8.2 ghi thẳng nó nằm ngoài mẫu | CÓ — `tests/test_vong_doi_phien.py` (5 test: `/end` trên phiên chưa phát ⇒ `cancelled` + `start_ts` vẫn None; `/cancel` idempotent không dời mốc đóng; phiên huỷ không mở lại được; phiên đã phát không huỷ được và được chỉ đúng đường thay thế; đóng phiên quan sát KHÔNG biến nó thành thí nghiệm) và `test_case_5_an_abandoned_observation_session_can_be_closed` (dò `/openapi.json` nên không đóng đinh tên endpoint). **Đã kiểm chứng cổng có răng:** gỡ cả hai đường đóng phiên → **4/14 test đỏ**. Migration đã chạy trên Postgres THẬT (`livelift-migrate up` → `down 1` → `up` lại, 21 test `tests/test_store_contract.py` xanh với `DATABASE_URL` thật); kiểm trực tiếp trên DB: CHECK chặn đúng `cancelled` có `start_ts`, và vẫn từ chối trạng thái rác |
| 12/09/2026 | **SERIOUS (liêm chính kết quả — gói C)** — `/experiment/summary` gộp **MỌI** phiên `status='ended'` có lịch gán, không phân biệt thật với chạy thử. Hai phiên dò lỗi của gói kiểm chứng (`probe409`, `probe-pin`) — mỗi phiên sống **1 giây**, không một cú nhấp — đã lọt **vĩnh viễn** vào kết quả gộp. Chủ shop bấm thử một lần cho quen tay là làm bẩn kết quả của chính mình, và không có nút gỡ (§2.4c) | Không tồn tại khái niệm "phiên chạy thử" ở tầng dữ liệu: `live_session` không có cột nào phân biệt mục đích phiên, nên bộ lọc mẫu chỉ có thể hỏi `status == 'ended' and not analysis_only`. Sâu hơn: quy tắc **phiên nào được tính** chưa từng được viết ra ở đâu — nó nằm ngầm trong một dòng list-comprehension của route, nên không ai kiểm được mẫu thật có khớp mẫu đã khai hay không | Cột `dry_run boolean NOT NULL DEFAULT false` (migration 0008) + trường `dry_run` trên `POST /sessions`. Ba ràng buộc làm nó là quy tắc tiền đăng ký chứ **không phải cửa hậu chọn lọc kết quả**: (a) khai báo lúc **TẠO** phiên — trước khi bốc lịch gán, trước khi tồn tại một con số nào; (b) **bất biến** — không endpoint nào nhận nó về sau và cả hai store đều bỏ qua nó trong `update_session` (`store._SESSION_WRITE_ONCE`, chung một danh sách cột write-once cho memory lẫn Postgres); (c) **mặc định là TÍNH VÀO** — một phiên thật biến mất âm thầm còn tệ hơn một phiên thử lọt vào. Bộ lọc gom về một hàm có tên `_in_analysis_sample` với bốn điều kiện, và `/experiment/summary` công bố `sessions_excluded` (lý do tiếng Việt → số phiên) vì một phiên bị loại mà không ai thấy thì không phân biệt được với một phiên chưa từng tồn tại. Quy tắc đầy đủ viết vào **PREREGISTRATION §8.2**, kèm câu cấm rõ: KHÔNG được loại một phiên THẬT sau khi đã chạy — phiên thật hỏng giữa chừng xử lý ở cấp KHỐI theo §8 | CÓ — `tests/test_vong_doi_phien.py` (quy tắc nạp mẫu quyết định được mà không nhìn bất kỳ kết quả nào; mọi loại trừ đều được ĐẾM và gọi tên; phiên chạy thử nằm ngoài kết quả gộp nhưng `/report` + `/bao-cao` vẫn xem được; phiên thật không bao giờ bị bỏ im lặng; **cờ không lật được sau khi đã chạy** — kiểm cả ở tầng store lẫn ở `/openapi.json`: không schema nào ngoài `SessionCreate` nhận cờ này) và `test_case_4_a_practice_run_never_pollutes_the_pooled_result` (dò tên trường trong `/openapi.json`). **Đã kiểm chứng cổng có răng:** bỏ điều kiện `dry_run` khỏi bộ lọc → 2 test đỏ |
| 12/09/2026 | **MINOR (mời thao tác vô nghĩa — gói C)** — `GET /sessions/{id}/state` trên phiên phân tích nước hoa (`analysis_only: true`, `status: ended`) vẫn trả **ba thẻ "Ghim ..."** ba sản phẩm demo trong kho (bình giữ nhiệt, khăn lau, sáp thơm) không liên quan gì tới video. Buổi live ấy của người khác và đã phát xong từ lâu. Bản báo cáo cùng phiên thì rất cẩn trọng ("KHÔNG có số nhân quả"), còn bảng điều khiển bên cạnh mời một cú bấm mà `/actions/execute` chắc chắn từ chối (§1.5) | `get_state` dựng thẻ từ danh mục sản phẩm **không điều kiện** — nó chưa bao giờ hỏi phiên này có ghim được không. Thẻ là một LỜI MỜI THAO TÁC, mà thao tác ấy chỉ tồn tại trong khối BẬT của phiên đang phát; điều kiện đó đã có sẵn trong `execute_action` nhưng không tầng nào chia sẻ nó với tầng hiển thị. Giao diện đã có sẵn ô trống tử tế cho phiên quan sát (`web/src/app/desk/page.tsx`), chỉ là nó không bao giờ được kích hoạt vì API luôn trả thẻ | Hàm thuần `cards.pin_cards_blocked_reason(status, analysis_only)` trả **lý do tiếng Việt** hoặc None, và là nguồn duy nhất cho cả hai phía: `/state` chỉ dựng thẻ khi lý do là None (ngược lại trả `cards: []` kèm trường mới `cards_note` nói vì sao — phân biệt "không mời vì không thao tác được" với "chưa đủ số liệu để xếp hạng", hai trạng thái đọc giống hệt nhau trên một danh sách rỗng không lời), và `/actions/execute` dùng đúng hàm đó cho câu từ chối. Nhánh gán của khối hiện tại **cố ý không phải đầu vào**: giấu thẻ trong khối TẮT sẽ biến danh sách thẻ thành một kênh lộ nhánh (rule L6) | CÓ — `tests/test_vong_doi_phien.py` (bảng chân trị của hàm thuần cho 5 trạng thái × cờ quan sát; danh sách thẻ rỗng và câu từ chối của `/execute` phải là **cùng một chuỗi**; và **cổng ngược** `test_a_live_session_still_gets_its_cards` để bản vá không làm bàn điều khiển câm đúng lúc nó phải nói) cộng `test_case_1_finished_replay_offers_no_pin_cards`. **Đã kiểm chứng cổng có răng:** dựng thẻ vô điều kiện trở lại → **3/14 test đỏ** |
| 12/09/2026 | **MINOR (cổng chất lượng đỏ — gói thẩm định)** — `ruff format --check src tests` báo `1 file would be reformatted` trên `tests/test_web_bat_dau.py:245`, tức cây làm việc do ba gói A/B/C bàn giao **không qua nổi chính cổng đã công bố là xanh**. `pytest`, `ruff check`, `tsc` và `check_isolation.py` đều xanh nên lỗi không lộ ra ở đâu khác | Tệp test mới của gói trước được viết tay rồi chỉ kiểm bằng `ruff check` (lint) — `ruff format --check` (định dạng) là **một lệnh khác**, không được chạy trên tệp mới thêm. Không có gì tự động buộc hai lệnh đi cùng nhau trên tệp chưa theo dõi trong git | Chạy `ruff format tests/test_web_bat_dau.py` (một biểu thức điều kiện trong list comprehension gộp lại đúng chuẩn), rồi chạy lại toàn bộ cổng: `ruff format --check src tests` → 110 tệp đã đúng định dạng; `pytest -m "not slow"` → 838 passed; `pytest -m slow` → 13 passed | không — `ruff format --check src tests` ĐÃ là gate bắt buộc trong quy trình; lỗi là do gói trước không chạy nó, không phải do gate thiếu. Bài học ghi lại ở đây: mọi tệp MỚI phải qua cả hai lệnh ruff trước khi bàn giao |
| 13/09/2026 | Trang chủ in "Chưa kết nối được máy chủ" TRONG KHI API đang chạy và trả lời `/health` trong 0,002 giây; chip chế độ trên thanh nav vẫn thản nhiên khoe DEMO/THẬT khi kho dữ liệu đã chết; máy chủ CHẬM và máy chủ CHẾT hiện cùng một câu | Probe hỏi SAI CÂU HỎI: trang chủ thăm dò sức khoẻ bằng `listSessions(2500)`, tức bằng một truy vấn ĐỌC KHO. Khi PostgreSQL chết, `GET /sessions` treo 30 giây rồi trả 500 nên probe 2,5 giây LUÔN hết giờ và trang kết luận "API chết". Cộng thêm hai thiếu sót cấu trúc: probe chỉ có HAI trạng thái (sống/chết) cho một thế giới có BA — máy chủ sống + kho suy giảm là trạng thái riêng — và 2,5 giây quá ngắn cho máy dev lạnh nên CHẬM bị kết luận nhầm là CHẾT, không có lần thử lại nào | `probeServer()` trong `web/src/lib/api.ts` hỏi `/health` (endpoint rẻ, không đọc kho), 4 giây, thử lại một lần, đọc CẢ thân lẫn mã HTTP nên 5xx vẫn được tính là đang sống; ba trạng thái SỐNG/SUY GIẢM/CHẾT với ba câu tiếng Việt khác nhau trong `SERVER_STATUS_MESSAGE` (câu "Chưa kết nối" chỉ tồn tại ở nhánh CHẾT); `ModeChip` đeo thêm chip cam KHO SUY GIẢM hoặc chip đỏ MẤT KẾT NỐI thay vì im lặng; `/ket-qua` phân biệt danh sách RỖNG với LỖI TẢI; `/replay` không còn gọi bản ghi mô phỏng là "DỮ LIỆU THẬT" | CÓ — `tests/test_web_probe.py` (20 test): khoá probe phải gọi `/health` chứ không phải `/sessions`, ba trạng thái ba câu khác nhau, `serverStatusOf` không bao giờ trả "down" khi máy chủ đã trả lời, timeout ≥ 4000 ms và ≥ 2 lần thử, `/health` không đọc kho nặng. Đã kiểm chứng gate FAIL khi cố tình trả probe về `/sessions` và về 2500 ms |
| 13/09/2026 | **SERIOUS (gói C)** — chủ dự án mở sản phẩm lên và thấy TRANG CHỦ RENDER THÀNH HTML THÔ, không CSS, không bố cục. `/_next/static/css/app/layout.css` trả 404. Cùng lúc đó 894 test nhanh, 13 gate chậm, `tsc --noEmit` và `ruff` đều XANH — không một cổng nào mở trang lên xem nó có mặc quần áo hay không | Hai nguyên nhân chồng nhau, cùng gốc là THƯ MỤC BUILD DÙNG CHUNG. (1) Nhiều tiến trình `next dev`/`next build` của các tác tử chạy song song cùng ghi một thư mục `.next`; Next không coi đó là lỗi nên không ai kêu, kết quả là `.next/static/css` RỖNG. (2) Một tiến trình `next` CŨ (PID là node, không phải docker) vẫn giữ cổng 3000 và tiếp tục phục vụ từ `.next` đã bị xoá, nên trình duyệt vẫn nhận HTTP 200 kèm thẻ `<link rel=stylesheet>` trỏ vào một tệp không còn tồn tại. Thẻ link còn nguyên là lý do mọi phép kiểm rẻ tiền đều xanh | `scripts/chay_local.py` — MỘT lệnh làm trọn vòng: báo PID+tên tiến trình giữ cổng 3000/8000 và chỉ dừng khi được đồng ý hoặc có `--force`, dọn thư mục build thừa `.next-*`, ĐO Postgres bằng `SELECT 1` rồi tự chọn kho và nói rõ dữ liệu nằm ở RAM hay không, đợi `/health` xanh, rồi đợi trang chủ 200 VÀ TẢI TỆP CSS VỀ CÂN, hỏng thì tự dọn thư mục build và dựng lại một lần. Thư mục build mặc định đổi thành `.next-chay-local` để không giẫm lên `.next` của người gõ tay; luật "mỗi tiến trình song song một LIVELIFT_DIST_DIR riêng" ghi thẳng vào `web/next.config.mjs` và `web/.gitignore` | CÓ — `scripts/gate_css_web.py` + `tests/test_web_css_gate.py` (slow): `next build` THẬT vào thư mục riêng, `next start`, rồi khẳng định trang chủ 200, có thẻ stylesheet, và TẢI được tệp CSS đó về với kích thước > 10 KB kèm token `--canvas`/`--brand`. Đo được: build 51,3s, CSS bản build 36.017 byte. **Đã kiểm chứng cổng có răng:** xoá tệp CSS khỏi bản build rồi phục vụ lại thì trang chủ VẪN 200 và thẻ link VẪN nguyên, nhưng cổng đỏ vì bốn lẽ độc lập (404, Content-Type HTML, trang lỗi chỉ 7.083 byte, thiếu cả hai token) — đúng hình dạng sự cố. Cộng `tests/test_khoi_dong_sach.py` khoá các hàm thuần và bất biến cấu hình |
| 13/09/2026 | **MINOR (gói C)** — không lệnh nào trả lời được câu "ai đang giữ cổng 3000"; tiến trình mới không lên, tiến trình cũ không chết, và người vận hành chỉ thấy `EADDRINUSE` | Không có công cụ nào trong kho mã hỏi hệ điều hành câu đó; ai cũng phải tự nhớ cú pháp `Get-NetTCPConnection`. Tệ hơn: mọi cách sửa nhanh đều là giết theo tên tiến trình (`taskkill /IM node.exe`), tức giết cả những tiến trình không liên quan | Bước (a) của `scripts/chay_local.py` in PID + TÊN từng tiến trình đang NGHE đúng cổng đó, rồi hỏi trước khi dừng; không có TTY và không có `--force` thì dừng lại và in hướng dẫn, TUYỆT ĐỐI không tự giết | CÓ — `tests/test_khoi_dong_sach.py`: bộ đọc `netstat -ano` phải phân biệt cổng 3000 với 30000 và với cổng 3000 ở cột Foreign Address (hai nhầm lẫn này đều kết thúc bằng giết nhầm tiến trình), và bất biến an toàn "không TTY + không --force thì KHÔNG giết" được khoá bằng test đếm số lần gọi hàm giết |
| 13/09/2026 | **SERIOUS (health nói dối — gói A-HEALTH)** — PostgreSQL chết hoàn toàn (không còn ai nghe ở cổng 5432) nhưng `GET /health` vẫn trả **200 trong 0,002 giây** với `storage_mode=postgres`, `durable=true` và câu *"Dữ liệu nằm trong PostgreSQL — khởi động lại không mất gì"*. Cùng lúc `GET /sessions` **treo đúng 30 giây** rồi trả `Internal Server Error` trần. Giữa một phiên live, người vận hành nhìn health thấy xanh và tin mọi thứ ổn, trong khi hệ thống không ghi nổi một dòng — và không một chữ nào trên dây nói rằng thứ chết là cơ sở dữ liệu | Ba lỗ hổng chồng nhau, cùng một gốc: **tuyên bố mà không kiểm chứng**. (1) `/health` chưa bao giờ HỎI kho lấy một câu — `durability_info()` chỉ đọc thuộc tính `store.backend` rồi khẳng định `durable=true`; với một kho mạng, tên backend nói kho nằm ở ĐÂU chứ không nói nó còn SỐNG hay không, nên câu trả lời là bịa đúng theo nghĩa nguyên tắc cốt lõi cấm. (2) Ba pha đi ra cơ sở dữ liệu đều chạy bằng mặc định thư viện: `ConnectionPool.timeout` = **30,0 giây** (chính con số đếm được), `connect_timeout` của libpq = **chờ VÔ HẠN**, không có `statement_timeout`. (3) Mọi hỏng hóc hạ tầng nổi lên nguyên dạng `psycopg.OperationalError` — không tầng nào dịch nó thành câu người đọc được, nên Starlette biến thành 500 trống | `PostgresStore.ping()` = `SELECT 1` có hạn **1,5 giây**, chặn CẢ hai pha (chờ pool cấp kết nối, và `SET statement_timeout` trong giao dịch để một máy chủ còn mở cổng nhưng đã lết không treo được /health). `storage_health()` = `durability_info` + **BẰNG CHỨNG**: ping thất bại ⇒ `status="degraded"`, `durable=false`, **gỡ** `storage_note`, và `storage_warning` nói thẳng *"KHÔNG kết nối được PostgreSQL — hệ thống đang KHÔNG lưu được dữ liệu, mọi ghi sẽ thất bại"* kèm hai đường sửa (bật lại DB / `STORE_BACKEND=memory`); trường `storage_ping` đính kèm độ trễ, hạn giờ, nguyên văn lỗi driver và cờ `cached`. **Cố ý giữ HTTP 200** cho /health (thân của nó chính là lời giải thích sự cố; trả 503 thì client vứt thân đi và lại không phân biệt được *API chết* với *DB chết* — đúng cái nhầm lẫn của hôm đó), cổng cho giám sát là `status != "ok"`; các route ĐỌC KHO thì ngược lại, chúng thật sự không làm được việc nên trả **503**. Hạn giờ mới, chỉnh được qua `.env`: connect **3s**, pool **2,0s** (cố ý NGẮN HƠN hạn 2,5s của trang web — trả lời sau khi trình duyệt đã bỏ cuộc thì câu tiếng Việt viết kỹ mấy cũng không ai đọc), statement **8s**, ping **1,5s**, đóng pool **1,0s**. Mọi đường ra DB đi qua một `_connection()` duy nhất, dịch `psycopg.OperationalError` (gồm cả `PoolTimeout`) thành `StoreUnavailableError`, và một `@app.exception_handler` biến nó thành 503 + `Retry-After` + nguyên văn lỗi driver để gỡ rối. Ping được đệm 5 giây (có `cached=true` tự khai) để trang web hỏi liên tục mà không đấm vào DB. **Đo trên chính máy có PostgreSQL đã chết**: /health 200 trong **1,547s** (lần hai **0,0025s**, `cached=true`), `/sessions` **503 trong 2,040s** — trước đó là 30 giây rồi 500 trần | CÓ — `tests/test_health_su_that.py` (17 test, giả lập DB chết bằng một cổng localhost không ai nghe, không cần Docker): /health khi kho chết phải `durable=false` + đúng câu cảnh báo + **dưới 3 giây**; một route đọc kho phải 503 có thân tiếng Việt + **dưới 5 giây**; `durability_info` vẫn thuần còn `storage_health` mới là hàm kiểm chứng; kho không có `ping` phải tự khai là không biết (không được mặc định coi là khỏe); chế độ memory không vạ lây; đệm ping không làm /health nói chuyện cũ và không trộn kết quả giữa hai kho; cổng cấu trúc cấm mượn `self._pool` ngoài `_connection()`. **Đã kiểm chứng cổng có răng:** (a) bỏ ping khỏi `storage_health` → **8/17 đỏ**; (b) trả pool timeout về 30,0 → **2 đỏ**, trong đó test `/sessions` đỏ vì vượt hạn 5 giây (đúng hình dạng sự cố); (c) gỡ lớp dịch lỗi → **2 đỏ**. Toàn bộ **983 test nhanh** xanh |
| 13/09/2026 | **Kho chết giữa phiên live** — Docker tắt, PostgreSQL biến mất, API vẫn sống. Ba hệ quả cùng lúc: (a) bộ thu vẫn thử đủ 3 vòng backoff cho MỌI bình luận dù kho đã chết nên nhịp đọc nghẽn ngay lúc khán giả đông nhất, và payload sai 400/422 cũng bị spool, gieo bản ghi "độc" khiến mọi lần nạp bù sau đó báo thất bại vĩnh viễn; (b) `POST /sessions/{id}/comments` trả `500 Internal Server Error` TRỐNG RỖNG — người vận hành không biết bình luận vừa rồi đã lưu hay chưa; (c) bộ thực thi tự động nuốt lỗi kho trong `except Exception`, quét tiếp mỗi 5 giây, IM LẶNG, để hết khối BẬT này đến khối BẬT khác trôi qua không ai ghim | Ba nguyên nhân độc lập, cùng một gốc "thiếu nguồn thì phải TUYÊN BỐ thiếu": (a) `ApiSink._post` chỉ có một chế độ — không phân biệt "máy chủ hỏng tạm thời" (phải giữ bản ghi, nhưng đừng bắt vòng đọc chờ nữa) với "payload sai vĩnh viễn" (gửi lại vô nghĩa); (b) đường ghi sự kiện không có lớp nào dịch lỗi kho thành câu tiếng Việt, nên lỗi psycopg rơi thẳng vào ASGI; (c) `autopilot.step_all` coi lỗi kho như mọi lỗi khác và `step_session` nuốt nó vào `hb.last_error` rồi trả None — đúng cách sinh ra BẢN GHI TUÂN THỦ SAI: báo cáo sẽ nói "khối BẬT không tuân thủ" như thể đội vận hành lười, trong khi thật ra cơ sở dữ liệu đã chết | (a) `ApiSink` có **chế độ spool**: sau 2 POST liên tiếp hỏng phía máy chủ (5xx hoặc đứt mạng) thì ghi THẲNG vào `data/spool/<phiên>.jsonl`, 30 giây mới dò lại MỘT lần; 5xx/lỗi mạng/401/404 luôn giữ bản ghi, chỉ 400/422 mới bị vứt kèm log ERROR; sink phơi `spool_mode`/`spooled`/`dropped`/`last_error` cho heartbeat của runner hét lên mỗi 60 giây, và dòng cuối của runner từ chối nói "xong" khi spool còn bản ghi. (b) mọi route trong `routes/events.py` đi qua `storage_guard` → **503** + `Retry-After` + `X-LiveLift-Storage: down` + câu tiếng Việt nói thẳng *"bình luận này CHƯA ĐƯỢC LƯU"* kèm lệnh nạp bù; bug thật vẫn nổ nguyên trạng nhờ bộ phân loại `is_storage_outage` (khớp tên lớp DB-API nên không cần import psycopg). (c) lỗi kho làm vòng quét **DỪNG NGAY** giữa chừng (không ghim nửa vời khi chưa đọc nổi bảng phơi nhiễm), ghi cửa sổ chết vào `storage_outage()` và bắn báo động lên `GET /sessions/{id}/state` — **nguyên nhân đứng trước hậu quả** trong câu báo động, và báo động Ở LẠI sau khi kho sống vì khối BẬT đã trôi qua không ghim bù được. **Đo thật** (`docs/benchmarks/kho-chet-giua-phien.md`, kịch bản chạy được `docs/benchmarks/kich-ban-kho-chet.py`): 20 bình luận tới trong lúc kho chết mất **30,582s** theo cách cũ so với **3,075s** cách mới (**9,9×**), 20/20 bản ghi vào spool, `dropped=0`; nạp bù `spool_replay` được 20/20 và chạy lần thứ hai KHÔNG nhân đôi (21 bình luận cả hai lần) | CÓ — `tests/test_kho_chet_giua_phien.py` (15 test, giả lập kho chết bằng một lớp bọc `InMemoryStore` ném đúng `OperationalError` của DB-API, không cần Docker): mọi mã 5xx đều phải vào spool; 400/422 phải bị vứt chứ không gieo bản ghi độc; chế độ spool phải cắt 12 lời gọi HTTP xuống 6 cho 4 bản ghi rồi dò lại đúng MỘT lần; đường ghi và đường đọc phải trả 503 có thân tiếng Việt; một `ValueError` thật KHÔNG được ngụy trang thành 503; vòng quét phải dừng với 0 `exposure_event` và báo động phải hiện trên `/state`; toàn tuyến kho-chết→spool→kho-sống→nạp-bù không trùng lặp. **Đã kiểm chứng cổng có răng:** khôi phục hành vi cũ ngay lúc chạy (bỏ `storage_guard`, trả `step_all` về vòng lặp nuốt lỗi, tắt chế độ spool + `PERMANENT_STATUSES`) → **9/15 test đỏ**. Toàn bộ **988 test nhanh** xanh |
| 14/09/2026 | **SERIOUS (số nói dối trên mặt tiền)** — trang chủ khoe *735 kiểm thử tự động đang xanh*, badge README ghi *249 test*, còn `pytest -m "not slow"` chạy ra **990**. Ba con số ở ba chỗ cho cùng một đại lượng. Hồ sơ thi lại trích 990 và tự nhận đã *hợp nhất số về một nguồn duy nhất* — tức chính hồ sơ nói sai về mình. Chú thích nguồn trong `web/src/app/page.tsx` còn tự mâu thuẫn trong hai dòng liền nhau: ghi *48.000 bình luận — 6 buổi live* ngay trên ô hiển thị *30+ buổi live* | Mỗi con số được chốt bằng tay vào lúc viết, rồi cũ đi theo một nhịp riêng. 735 lại còn không cùng THƯỚC ĐO với 990: nó là số hàm `def test_` đếm bằng grep, không phải số test pytest thu thập (tham số hóa làm một hàm sinh nhiều test). Không cổng nào so ba chỗ đó với nhau, vì cả ba đều là chữ trong tài liệu chứ không phải mã chạy được | Chốt nguồn duy nhất là chính pytest. `scripts/dong_bo_so_test.py` gọi `--collect-only` cho cả hai nhóm marker rồi ghi lại con số vào badge README, dòng lệnh mẫu, đoạn Quality gates và hằng `PROOF` của trang chủ; `--xem-truoc` trả mã thoát khác 0 khi có chỗ lệch. Bộ số mặt tiền đổi sang **19.126 bình luận · 16 buổi live · 993 kiểm thử**, cố ý chọn con số NHỎ HƠN nhưng truy được về đúng một lô đo (10/09) thay vì con số cộng dồn không tài liệu nào chứng minh | Chưa — cổng tự động sẽ đỏ mỗi lần thêm một test, phiền hơn giá trị nó mang lại. Thay bằng luật vận hành: chạy `dong_bo_so_test.py --xem-truoc` trước mỗi lần nộp hồ sơ hoặc dựng bản demo |
| 14/09/2026 | **SERIOUS (buổi demo mở ra là trống)** — bộ phiên demo vàng chỉ có bản CLI. Với kho `memory` (cấu hình mặc định khi chưa dựng PostgreSQL, và là cấu hình một máy lạ sẽ chạy), `scripts/seed_demo_vang.py` dựng store trong tiến trình của chính nó, gieo 6 phiên vào đó rồi thoát. API đang chạy không thấy phiên nào. Phát hiện lúc dựng kịch bản demo cho hội đồng: mở `/ket-qua` ra thì mục phiên demo trống trơn | Bộ vàng được thiết kế như một script gieo vào kho BỀN VỮNG, nên không ai hỏi "gieo vào kho nào" khi kho là bộ nhớ của một tiến trình khác. Mọi test của bộ vàng đều gọi thẳng hàm `seed_demo_vang(store)` với store tự tạo, nên không test nào đi qua đường HTTP — đúng khoảng mù đó | Thêm `POST /demo/seed-vang` gieo vào `StoreDep`, tức kho thật của tiến trình đang phục vụ; một máy vừa dựng xong có đủ ba trạng thái kết quả sau một lệnh. Lỗi trạng thái vẫn vỡ to như ở CLI (`DemoVangStateError` không bị bắt) — thà 500 còn hơn giao bộ demo kể sai câu chuyện | CÓ — `test_demo_vang_seeds_into_the_running_api_store`: gieo qua HTTP rồi khẳng định 6 phiên xuất hiện trong `/sessions?env=demo` và đọc được `/sessions/{id}/bao-cao` đúng trạng thái. **Đã kiểm chứng cổng có răng:** đổi đường dẫn route → test đỏ với 404 |
| 14/09/2026 | **MINOR (demo nhân bản)** — gieo dữ liệu mẫu lần thứ hai sinh thêm một bộ trùng tên y hệt. Ảnh chụp `/ket-qua` cho **12 dòng mang đúng 6 cái tên**; kho đang chạy tích lũy 22 phiên thừa qua các lần gieo trước đó (4 bản mỗi tên cho nhóm *Phiên mô phỏng seed=…*). Giám khảo nhìn vào không biết dòng nào là dòng nào | Tên phiên là hằng theo seed (`Phiên mô phỏng seed={seed}`) nên mọi lần gieo đặt cùng một tên. Trớ trêu là mã link đã xử đúng từ sự cố 27/08 — `_seed_products_and_links` gắn nhãn ngẫu nhiên mỗi lần gieo VÌ "bấm Xem thử ngay nhiều lần là thao tác bình thường" — nhưng lý lẽ ấy không được áp sang tên phiên | `/demo/seed-vang` bất biến: đã có bộ vàng thì trả lại bộ đang có kèm câu giải thích, muốn bộ mới thì `?gieo_lai=true`. `/demo/seed` dùng chung nhãn lần-gieo với mã link, tên thành `Phiên mô phỏng #1 · gieo 14/09 21:45·a3f2`. Nhãn phải có phần NGẪU NHIÊN chứ không chỉ giờ-phút: bản vá đầu chỉ dùng giờ-phút và cổng kiểm thử bắt ngay — bấm hai lần liên tiếp thì hai lần rơi cùng một phút. Kho đã lỡ nhân bản thì dọn bằng `scripts/don_phien_demo_trung.py` (chỉ đụng phiên `is_demo`, giữ bản tạo sớm nhất, sao lưu trước khi ghi); kho đang chạy giảm 49 → 27 phiên | CÓ — `test_seed_vang_twice_does_not_duplicate_the_golden_set` và `test_seeding_sample_sessions_twice_gives_distinguishable_titles`. Cổng thứ hai đã bắt đỏ chính bản vá đầu tiên của nó |
