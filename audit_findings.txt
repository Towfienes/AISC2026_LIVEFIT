written 16
L] NaN observed statistic is silently treated as "more extreme than every redraw": p = 1/(draws+1) and a zero-width CI on pure noise
FILE: src/livelift/analysis/estimators.py:61,148,177
WHY: studentized_stat returns float('nan') when either arm has fewer than 2 blocks (line 61). randomization_test then computes p = (1 + sum(|t_draws| >= |nan|)) / (1 + n_draws); every comparison with NaN is False, so the count is 0 and p collapses to the floor 1/(n_draws+1). The identical expression inside randomization_ci.pval (line 177) returns that same sub-alpha value for EVERY candidate tau0, so the expansion loop breaks immediately, the bisection sends `hi` to `lo` on every iteration, and search() returns lo = tau_hat in both directions -> CI = [tau_hat, tau_hat]. A configuration that carries no information about the effect is therefore reported as the most confident possible result: point 
FIX: Guard before inference: if min(n_on, n_off) < 2, return p=NaN / an explicit 'insufficient blocks per arm' result and no CI, and have reports.py refuse to publish a summary in that case. In randomization_test/randomization_ci, treat a non-finite t_obs as a hard error rather than letting NaN comparisons yield the p-value floor. In _batch_studentized, return NaN for rows with n1<2 or n0<2 and exclude them from the reference distribution (recording how many were dropped) instead of mapping them to 0.0. Separately, enforce the design constraint (min_per_arm_per_phase) as a minimum-session-length precondition so 3-block sessions cannot be scheduled at all.

=== 2. [FATAL] Blocks scheduled after the host actually ends the stream get fabricated exposure and a hard y=0.0, attenuating the headline estimate ~38% and dropping Fisher-CI coverage to 72%
FILE: src/livelift/api/routes/reports.py:48
WHY: The analysis timeline is built from planned_duration_min, never from the real session end. service.rebuild_schedule (service.py:167) rebuilds the Schedule with session["planned_duration_min"], and block_frame (features.py:170) sets session_s = schedule.session_duration_min * 60. build_ticks then materialises ceil(session_s/30) buckets and CARRIES FORWARD last_viewers (features.py:82,89,96) to the end of the PLANNED session — the last real viewer snapshot (e.g. 400 concurrent viewers) is propagated over every minute after the host stopped broadcasting. So each block after the true end has exposure_viewer_s >> 0 and clicks = 0, giving y = clicks/exposure*1000 = 0.0 (features.py:196). Nothing c
FIX: Carry the real broadcast span into the analysis. Pass live_until_s = (end_ts - start_ts).total_seconds() from _session_frame into block_frame, clip every analysis window to min(b.end_offset_s, live_until_s), and stop build_ticks carrying last_viewers past the last observed snapshot (emit no ticks beyond it rather than forward-filling to the planned end). Any block whose surviving live window is shorter than a pre-registered threshold must be dropped, not reported as y=0.0.

=== 3. [SERIOUS] studentized_stat's exact `se == 0` test misses floating-point dust, producing t_obs ~ 1.8e16 and a spurious rejection that collapses the Fisher CI to zero width
FILE: src/livelift/analysis/estimators.py:63-65
WHY: The zero-variance guard is an exact equality (`if se == 0: return 0.0`). During CI inversion the adjusted outcome is y_adj = y - tau0*z; when y is constant, y_adj takes exactly two values and each arm is internally constant, so the within-arm variance is mathematically 0 — but numpy computes var as mean-of-squared-deviations around a float mean, which for magnitudes like 7e-06 leaves denormal residue instead of exact 0. se then equals ~4e-22 rather than 0, the guard does not fire, and t_obs = (m1-m0)/se explodes to ~1.8e16. No redraw can match it, so pval(tau0) = 1/(n_draws+1) < alpha: a spurious rejection at an arbitrary point of the search path. Since search() takes the FIRST rejected tau0
FIX: Replace the exact test with a scale-aware one, e.g. `if not np.isfinite(se) or se <= 1e-12 * max(1.0, abs(m1)+abs(m0)): return 0.0`, and apply the same tolerance in _batch_studentized's `np.where(se > 0, ...)`. Additionally, make the CI search robust to isolated dips: require k consecutive rejected grid points (or a monotone smoothing of pval) before accepting a bracket, and use a common-random-number p-value that is evaluated on a fixed grid so the non-monotonicity is visible rather than silently exploited by bisection.

=== 4. [SERIOUS] When viewer telemetry starts late, clicks are counted over the full window but exposure only over the ticked part — inflating that block's rate by up to 26%
FILE: src/livelift/core/features.py:138
WHY: _window_stats measures the numerator and the denominator over different effective spans. Clicks are counted straight off the raw event list with only a time predicate (features.py:138: `start_s <= e.ts_offset_s < end_s`), independent of any tick coverage. Exposure, however, is integrated from the carried-forward tick series (features.py:143-151), and build_ticks initialises `last_viewers = 0.0` (features.py:82) — so every second before the FIRST viewer_count snapshot contributes exactly 0 viewer-seconds. Clicks in that span are still counted. The result is y = clicks/exposure*1000 with a denominator covering a strictly shorter interval than the numerator, i.e. a systematically inflated rate.
FIX: Make tick coverage explicit rather than implicit. Track the first observed snapshot time and treat [window_start, first_tick) as UNMEASURED: either restrict the click count to the ticked span (so numerator and denominator share support) or refuse the block via the minimum-exposure rule. Do not let `last_viewers = 0.0` silently mean "zero viewers" when it actually means "no data"; carry a sentinel/None and integrate only over covered intervals.

=== 5. [SERIOUS] Cluster design effect 1+(m−1)·ICC applied to a design where treatment is randomized WITHIN the cluster
FILE: src/livelift/analysis/power.py:46-47,54
WHY: deff_cluster = 1 + (m-1)*icc_session with m = blocks per session is the Moulton/cluster design effect for CLUSTER-LEVEL assignment — all m units in a cluster share one arm, so the cluster random effect does not cancel and inflates the contrast variance. In a switchback the session is the cluster and the treatment alternates INSIDE it. The session random effect α_s is common to that session's ON and OFF blocks and is differenced out of the ON−OFF contrast; the pre-registered analysis makes this explicit (ols_fe_lin demeans within session; _redraw_matrix redraws within session). The correct within-session dependence term for this design is the residual serial correlation, which the module alre
FIX: Set deff_cluster = 1 for the within-session switchback (drop icc_session from mde_relative, or gate it behind a flag used only for a hypothetical session-level-randomized arm). Model within-session dependence once, through the residual serial-correlation term computed for the treatment contrast — not twice.

=== 6. [SERIOUS] Serial-correlation factor √((1+ρ)/(1−ρ)) inflates the MDE, but serial correlation slightly HELPS the switchback contrast
FILE: src/livelift/analysis/power.py:48-49,55
WHY: (1+ρ)/(1−ρ) is the variance inflation factor for the MEAN of an AR(1) series of length n — the right correction when the comparison is over time (before/after, trend). Here the estimand is a contrast between interleaved blocks whose treatment indicator is drawn i.i.d. Bernoulli independently of the errors. Under that assignment the cross-block covariance terms enter the contrast variance with alternating signs and largely cancel; with positive ρ the ON and OFF means become positively correlated with each other, so Var(ȳ_ON − ȳ_OFF) DECREASES rather than increases. Multiplying the MDE by √((1+ρ)/(1−ρ)) therefore applies an inflation of the wrong sign, on top of the deff_cluster error which po
FIX: Derive the serial-correlation adjustment for the randomized contrast (for i.i.d.-per-block assignment it is ≈1; for designs with alternation constraints derive it from the realized assignment autocorrelation), or drop the term and state that block-level serial correlation is neutralized by within-session randomization. Do not import the AR(1) mean-inflation factor.

=== 7. [SERIOUS] reports.py feeds a POOLED cross-session CV into the power table, then multiplies by a session-ICC design effect — session variance counted twice
FILE: src/livelift/api/routes/reports.py:166,170-176
WHY: cv = y.std(ddof=1)/y.mean() is computed over the concatenation of every block from every session, so Var(y) = σ²_between-session + σ²_within-session. The primary analysis is within-session (RI redraws per session, OLS with session FE, cluster-robust by session) and therefore has no exposure to σ²_between. Passing the pooled CV already smuggles the session variance component into the MDE base; scenario_table then applies deff_cluster = 1+(m−1)·0.05 on top, which is a second, explicit charge for the same variance. Whichever of the two you believe, doing both is indefensible.
FIX: Compute the within-session CV — pool the session-demeaned residuals (with the FE degrees-of-freedom correction) over the grand mean — and pass that; drop the ICC term per the first finding. Remove the round(cv, 2) so the reported number is the one that was measured.

=== 8. [SERIOUS] The calibration world contains none of the dependence structure the design exists to handle — session ICC in the outcome is 0, block-level serial correlation is 0, and 93% of block variance is pure counting noise
FILE: src/livelift/sim/simulator.py:46
WHY: The headline "our estimator recovers a known effect with ~0% bias and ~95% coverage" is only informative if the simulated data violate the estimator's convenience assumptions somewhere. They do not. Three mechanisms cancel: (1) the session shock (session_shock_sd=0.25, simulator.py:121) multiplies the ARRIVAL rate only (simulator.py:137), but the primary outcome is a per-viewer-second RATE, y = clicks/exposure*1000 (features.py:196) — so the session random effect divides straight out of the outcome. Measured over 300 sessions x 16 blocks: session-level ICC of y = -0.0037, while ICC of viewer-seconds = +0.43. The session shock is real but lives entirely in a quantity the outcome normalizes aw
FIX: Put the heterogeneity where the outcome can see it: make the session shock (and a block-level random effect) multiply the per-viewer click probability p_click_tick, not just the arrival rate, so the outcome actually has a session ICC; and re-parameterize ar1_rho as a half-life in seconds (e.g. rho = 0.5**(tick_s/halflife) with halflife on the order of the block length) so block-level autocorrelation is non-trivial. Then re-run the bias/coverage gates and report the numbers under that world. Also add a gate that the naive i.i.d. estimator FAILS on the simulated data — if nothing fails, the world is too friendly to be evidence.

=== 9. [SERIOUS] No gate ever runs with carryover > 0, so the advertised bias/coverage numbers are valid only under NO interference — the exact assumption switchback designs exist to relax
FILE: tests/test_sim_validation.py:54
WHY: SimParams.carryover_halflife_s defaults to 0.0 (simulator.py:45), run_validation falls back to `sim_params or SimParams()` (validate.py:58), and every one of the four tests in tests/test_sim_validation.py either passes SimParams() or SimParams(treatment_effect=...) — none sets carryover_halflife_s. A grep across the whole repo shows the knob is set in exactly one place outside the simulator: the CLI flag (sim/cli.py:34), which no test or gate invokes. So the calibration claim is unconditionally stated but conditionally true: it holds in a world with zero interference between blocks. The design documents (docs/research/2026-08-24-switchback-design.md, PREREGISTRATION §61) all say the carryove
FIX: After fixing the estimand (previous finding), add an explicit gate over a carryover grid (e.g. halflife in {0, 60, 120, 300}s) asserting bias and coverage against the exposure-consistent truth, and report the resulting bias-vs-carryover curve as a figure. Where the estimator does degrade, say so — a documented breaking point is far stronger evidence than an undocumented pass. At minimum, restate the headline as "under the pre-registered no-interference assumption" everywhere it appears.

=== 10. [MODERATE] Horvitz-Thompson estimate is un-normalized (divided by n instead of the realized weight sums), so it swings with arm imbalance and is published next to a CI that belongs to a different estimator
FILE: src/livelift/analysis/estimators.py:53,218
WHY: ht_effect divides each arm's inverse-propensity sum by n rather than by the realized sum of weights (the Hajek normalization). With p == 0.5 the estimator is unbiased, but its sampling error picks up a term proportional to (n_on - n_off) times the LEVEL of y, which has nothing to do with the treatment effect. Under a Bernoulli switchback the arm counts are random, so this term is pure added noise: the HT estimate can differ from the difference-in-means by more than the entire effect being studied, including flipping sign. analyze_outer publishes it as `estimate_ht` alongside `estimate` (difference in means) and a Fisher CI / p-value that were computed for the difference-in-means statistic on
FIX: Report the Hajek/normalized estimator: (sum(y*z/p)/sum(z/p)) - (sum(y*(1-z)/(1-p))/sum((1-z)/(1-p))), which reduces exactly to the difference in means at p == 0.5; keep the raw HT only as a diagnostic, and if it is displayed, attach its own design-based variance rather than the difference-in-means CI. Also pass the per-block propensities recorded in the block frame instead of the hardcoded 0.5 at line 218.

=== 11. [MODERATE] Shortlink clicks that arrive after the broadcast has ended are attributed to randomized blocks
FILE: src/livelift/api/routes/reports.py:40
WHY: _events_from_store maps every click row in the session to `offset = (c["ts"] - start).total_seconds()` (reports.py:40) with no upper clip at end_ts, and block_frame then assigns it to whatever block's [win_start, end_offset_s) contains that offset. Meanwhile the redirect keeps logging clicks indefinitely: follow_shortlink (redirect.py:44-70) only skips the block_id tag when status != 'live' — it still calls store.add_click with a live `ts`. Shortlinks stay valid after the stream ends (they are pasted into the video description, the Facebook post, chat replays), so a genuine post-stream click stream lands inside the planned-but-dead tail of the schedule. Combined with the fabricated tail expo
FIX: Clip the event window in _events_from_store to the real broadcast span: skip any click/comment/tick whose offset is < 0 or > (end_ts - start_ts). Independently, have follow_shortlink stop attributing clicks to an experiment once the session status is 'ended' (record them, but flag them out of the experimental outcome), and add a QC counter for post-session clicks so the number is visible rather than silently absorbed.

=== 12. [MODERATE] pre_viewers / pre_comment_rate / pre_like_rate are post-treatment with respect to the previous block, and rerandomization makes Z_k correlate -0.17 with Z_{k-1}, so the CUPED docstring's "cannot leak treatment effect" guarantee is false
FILE: src/livelift/core/features.py:178
WHY: block_frame sets pre_start = b.start_offset_s - pre_window_s with pre_window_s = 120 (features.py:178). The minimum measurement-block duration under the default design is 240 s, so the trailing window lies ENTIRELY inside block k-1 — a randomized, treated interval. These fields are therefore outcomes of Z_{k-1}, yet features.py:123-125 labels pre_like_rate "Used ONLY as a pre-treatment covariate" and cuped_adjust's docstring (estimators.py:353-354) asserts "x must be pre-treatment ... so the adjustment cannot leak treatment effect". That would be harmless if Z_k were independent of Z_{k-1}, but draw_assignments (outer.py:150-186) rerandomizes until each phase stratum holds >= 2 of each arm, 
FIX: Either (a) build the covariates from a window that is genuinely pre-randomization — session-level or phase-level pre-period features fixed before any assignment is drawn — or (b) if a trailing within-session window is kept, condition on Z_{k-1} explicitly (include lagged assignment as a regressor) and state in the pre-registration that the estimand is a direct effect, and re-derive the adjustment under the rerandomized joint law rather than assuming i.i.d. assignment. Correct the two docstrings that assert the covariates are pre-treatment. Emit pre_* as None (not 0.0) for block 0 and exclude it from any theta fit.

=== 13. [MODERATE] Burn-in penalty applied twice: measured_cv is already computed on post-burn-in outcomes, then burn_in_share=0.2 inflates it again
FILE: src/livelift/api/routes/reports.py:25,54,166,170
WHY: _session_frame calls block_frame(..., burn_in_s=BURN_IN_S) with BURN_IN_S = 60, so every y already reflects the shortened analysis window — the variance cost of dropping the first 60 s is baked into the measured CV. experiment_summary then hands that CV to scenario_table, whose burn_in_share defaults to 0.2 and is never overridden, and mde_relative multiplies by √(1/(1−0.2)). The same loss of effective exposure is charged to the MDE twice.
FIX: Pass burn_in_share=0.0 from reports.py when the CV was measured on post-burn-in outcomes (and document that mde_relative's burn_in_share is only for planning with a pre-burn-in CV), or compute the CV on full-block outcomes and keep the correction. Pick one place.

=== 14. [MODERATE] The advertised 'at least 2 blocks per arm per phase' rerandomization constraint silently degrades to 1 (or 0) at standard session lengths
FILE: src/livelift/core/assigner/outer.py:170
WHY: required = min(min_per_arm_per_phase, len(idx)//2) caps the constraint at what the stratum can hold. Endpoint doubling consumes 4L of the timeline in 2 blocks, so the early and late thirds contain far fewer blocks than the nominal duration/L suggests, and the cap binds at ordinary session lengths — not just pathological ones. The design section of the module docstring and the plan both state the guarantee as 'at least 2 blocks of each arm per phase'; the realized schedule does not deliver it, nothing records the realized `required`, and nothing warns the operator. The published power scenarios all use 90 min, where the guarantee does hold, so the documentation and the shipped defaults descri
FIX: Record the realized `required` dict on the Schedule (alongside n_redraws) and surface it in the session report and the pre-registration; refuse or loudly warn when a requested session_duration_min cannot support min_per_arm_per_phase in every phase; state the guarantee as duration-dependent in the docstring.

=== 15. [MODERATE] The A/A gate cannot detect a materially miscalibrated test, has no lower bound, and the coverage gate fails a correctly-calibrated estimator ~39% of the time
FILE: tests/test_sim_validation.py:50
WHY: With n_reps=40, rejection_rate is a multiple of 1/40, so `assert rejection_rate <= 0.15` fails only when 7 or more of 40 replications reject. Binomial power: a test whose true false-positive rate is 12% (2.4x the nominal alpha) passes the gate 80.2% of the time; 15% (3x nominal) passes 60.7%; even a badly broken 20% FPR passes 28.6%. So the gate as written licenses an FPR anywhere up to roughly 3x alpha. It also has no lower bound, so it is silent about over-conservatism, even though HARNESS.md states the criterion as "rejection rate ~ alpha" — a two-sided claim. Symmetrically, the coverage gate `0.88 <= ci_coverage <= 0.99` at 40 reps requires 36 <= covered <= 39: an estimator whose true co
FIX: Raise n_reps for the A/A gate to at least 400 (it costs ~22s for 40 reps at these settings, so 400 is minutes, not hours) and assert a two-sided Clopper-Pearson interval on the rejection rate that contains alpha and excludes 2x alpha — e.g. assert the exact binomial 99% CI for the FPR lies within [0.02, 0.09]. Do the same for coverage (two-sided exact interval containing 0.95). Report the interval, not the point estimate, in the paper; "FPR 4.8% [3.0%, 7.2%] over 1000 A/A replications" is a claim a judge cannot dismiss, whereas "<= 0.15 over 40 reps" invites the attack.

=== 16. [MINOR] measured_compliance is computed and returned but never fed into the power table, which uses hardcoded compliance and unmeasured ICC/ρ/R²
FILE: src/livelift/api/routes/reports.py:170-176,190
WHY: power.py's own HARD RULE states that CV, ICC and ρ_r fed into the module must be MEASURED from pilot sessions. experiment_summary measures compliance (line 190) and CV, but passes only the CV; Scenario(..., compliance=0.95) and Scenario(..., compliance=0.85) are literals, and icc_session=0.05, resid_autocorr=0.2, var_reduction_r2=0.3 all fall through to scenario_table's defaults. The response therefore places a field literally named measured_compliance next to a power table that ignored it. The var_reduction_r2=0.3 default is the anti-conservative one: it multiplies the MDE by √0.7 = 0.837, a 16% improvement claimed on an assumed CUPED R² that no code in this path ever estimated — even thoug
FIX: Pass the measured compliance into Scenario, and pass var_reduction_r2 from an actual cuped_adjust run on the pre-block covariates (0.0 if CUPED is not in the reported pipeline). Return the ICC and ρ actually used alongside the table, or mark the rows as assumption-based until those are measured.
